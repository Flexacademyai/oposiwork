import 'package:flutter/material.dart';
class StudyCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Calendario de estudio")),
    );
  }
}
